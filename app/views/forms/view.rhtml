<h2><%= @form.instance.visual_identifier %></h2>
<p>(Form type <em><%= @form.type %></em>) <%= link_to 'Edit', doctor_forms_path(:action => 'draft', :form_status => 'draft', :form_type => @form_type, :form_id => params[:form_id]), :confirm => "Editing this form will un-submit the form and move it back to the Draft status. Do you want to continue?" %>
</p>

<div class="form">
<%= render :partial => @form.instance.form_data_type.fromCamelCase -%>
</div>

<%= render :partial => 'form_notes', :locals => {:form => @form} %>
